# Architecture

EasyCode is built as a local-first AI assistant with:

- FastAPI backend for orchestration
- Ollama as the model runtime
- VS Code extension frontend
- SQLite session and checkpoint storage
- Search and indexing support for repository context
