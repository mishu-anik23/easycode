[tool.poetry]
name = "easycode"
version = "0.1.0"
description = "Local-first AI coding assistant MVP"
authors = ["EasyCode Team <team@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.110.0"
uvicorn = {extras=["standard"], version="^0.22.0"}
pydantic = "^2.7.0"
requests = "^2.31.0"
sqlalchemy = "^2.0.0"
python-dotenv = "^1.0.0"
pydantic-settings = "^2.7.0"

[tool.poetry.dev-dependencies]
pytest = "^8.0.0"
httpx = "^0.27.0"
