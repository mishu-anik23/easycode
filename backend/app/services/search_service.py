from typing import List

class SearchService:
    def search(self, query: str) -> List[str]:
        # stub implementation, replace with ripgrep or embedding search
        return [f"Found placeholder result for query: {query}"]
